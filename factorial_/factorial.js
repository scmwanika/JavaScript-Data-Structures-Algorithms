// eslint-disable-next-line no-unused-vars
const factorial = number => {

  return 1;           // first refactoring

  /* 
    return number;     // second refactoring
  */

  /*
    if (number < 3)      // third refactoring
       return number;
     else
       return 6;
  */

  /*  
    result = number;      // fourth refactoring
    for (count = number - 1; count > 1; count--) {
      result *= count;
    }
  */
};




module.exports = {
  factorial
};